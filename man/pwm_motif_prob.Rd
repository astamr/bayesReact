% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pwm_motif_prob.R
\name{pwm_motif_prob}
\alias{pwm_motif_prob}
\title{Calculate PWM motif probabilities and motif counts}
\usage{
pwm_motif_prob(
  motifs,
  motif_type,
  seqs,
  seqlist,
  threshold_type = "relative",
  threshold = 0.8,
  pwm_background = "seqs_nt_freq",
  pseudocount = 0.25,
  log_base = 2,
  motif_overlap = FALSE,
  prob_method = "global",
  markov_order = 0,
  dp_resolution = 10000,
  cores = parallel::detectCores()
)
}
\arguments{
\item{motifs}{list of motif matrices or aligned binding-site vectors,
e.g., list(motif_1 = motif_1_input, motif_2 = motif_2_input) or list(motif_1_input, motif_2_input).}

\item{motif_type}{character specifying the motif representation. Should be either "motif_sites", "pcm", "ppm", or "pwm".}

\item{seqs}{a dataframe with the columns: 'gid', 'sequence', and optionally 'nchar' (generated by build_seq_list()).}

\item{seqlist}{list of processed sequences generated by build_seq_list().}

\item{threshold_type}{character specifying how PWM score thresholds are defined. "relative" uses a fraction of the possible PWM score range (default); and "absolute" uses the provided raw PWM score.}

\item{threshold}{numeric threshold used to determine whether a sequence window is classified as a motif occurrence. Can be specified as a single value or vector with one value per motif. Relative thresholds must be between zero (loose) and one (stringent).
Resulting cutoff is enforced on the summed PWM scores in a sequence window, and motif occurrence is defined as score sum > cutoff (threshold = cutoff in 'absolute' setting).}

\item{pwm_background}{character specifying the nt background probabilities used when constructing or reconstructing PWMs/PPMs.
"eq_nt_freq" uses equal nt frequencies and "seqs_nt_freq" uses the nt frequencies pooled across all sequences.}

\item{pseudocount}{numeric pseudocount added to each PCM entry.}

\item{log_base}{numeric base used for log-odds PWM scores (match provided PWM or use default).}

\item{motif_overlap}{logical specifying whether overlapping PWM motif occurrences should
be counted.}

\item{prob_method}{character specifying how the per-window motif probability P(m) is estimated.
"exact" enumerates all nt paths through the PWM (slow);
"dp" approximates the PWM score distribution using discretization and dynamic programming (faster);
and "global" pools motif counts across all sequences into one per-window probability used for every sequence, correcting only for seq length not nt context (rapid).}

\item{markov_order}{integer specifying order of the sequence-specific motif background model used by "exact" and "dp": 0 uses nucleotide frequencies (default) and 1 uses dinucleotides.}

\item{dp_resolution}{integer specifying number of score bins used to discretize the PWM score range in DP approx. (high resolution leads to better approx. but slower run time).}

\item{cores}{integer specifying number of cores used to process motifs (set to 1 for Window systems due to use of mclapply).}
}
\value{
list containing sequences X motifs matrices: motif_counts and motif_probs.
}
\description{
Function for calculating PWM motif probabilities using a global or
sequence-specific binomial approximation and counting observed PWM motif occurrences.

Motifs should be supplied as a list of one of the following input types: Vectors with aligned binding-site sequences; or matrices with position counts (PCMs), position probabilities/frequences (PPMs), or
unscaled log-(odds+1) position weights (PWMs). Input motif matrices should use row order A, C, G, and T.
}
\examples{
\dontrun{
# Character vectors of aligned motif-binding sites.
motif_sites <- list(RBP_1 = c("ACGT", "ACGT", "ATGT"))
pwm_motif_prob(motifs = motif_sites, motif_type = "motif_sites",
seqs = seqs, seqlist = seqlist)

# Named lists of position count, probability, and log-odds weight matrices.
pcm_matrix <- matrix(c(8, 1, 1, 0, 0, 8, 1, 1,
                       1, 1, 8, 0, 0, 1, 1, 8),
                     nrow = 4,
                     dimnames = list(c("A", "C", "G", "T"), NULL))
pcm <- list(TF_1 = pcm_matrix)
ppm <- lapply(pcm, function(x) sweep(x + 0.25, 2, colSums(x) + 1, "/"))
pwm <- lapply(ppm, function(x) {
  log2(sweep(x, 1, c(A = 0.25, C = 0.25, G = 0.25, T = 0.25), "/") +1)})

pwm_motif_prob(motifs = pcm, motif_type = "pcm",
seqs = seqs, seqlist = seqlist)
pwm_motif_prob(motifs = ppm, motif_type = "ppm",
seqs = seqs, seqlist = seqlist)
pwm_motif_prob(motifs = pwm, motif_type = "pwm",
seqs = seqs, seqlist = seqlist)
}



}
